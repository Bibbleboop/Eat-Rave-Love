import React from "react";
import "./searchbar.css";



/*const searchbar=()=>{
    return (
        <div className="searchbar">
            <input></input>
        </div>
    )
}

import { useState, useEffect } from 'react';
import searchbar from './search bar';
import './App.css';

const getFilteredEvents = (query, events) => {
  if (!query) {
    return events;
  }
  return events.filter(events => events.name.includes(query))
};

function App() {
  const [query, setQuery] = useState('');

  const {events} = searchbar;
  const {results} = events;

  const filteredEvents = getFilteredEvents(query, events);

  return (
    <div className='App'>
      <label>Search</label>
      <input type="text" onChange={e =>setQuery(e.target.value)} />
      <ul> 
        {filteredEvents.map(value => (
        <h3 key={value.name}>(value.name)</h3> ))}
      </ul>
    </div>
  );
}

export default App;*/
